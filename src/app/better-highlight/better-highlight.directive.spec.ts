import { BetterHighlightDirective } from './better-highlight.directive.ts';

describe('BetterHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new BetterHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
